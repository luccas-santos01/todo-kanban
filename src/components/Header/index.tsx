import React from "react";
import { HeaderContainer } from "./Header.styles";

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <h2>Lista de tarefas de bolso</h2>
    </HeaderContainer>
  );
};

export default Header;
